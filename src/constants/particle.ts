import { RGBType } from '../types/rgb'

export const PARTICLE_CAPTURED = {
  COLOR: [12, 222, 42] as RGBType,
  MAX_TIME: 300,
  REDUCE_FACTOR: 0.25,
} as const

export const PARTICLE_INCREMENT_YELLOW_TOWER_PROGRESS = 10
